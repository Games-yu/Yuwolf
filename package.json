{
  "name": "werwolf-duesterwald",
  "version": "1.0.0",
  "private": true,
  "description": "Echtzeit-Mehrspieler Werwolf für Railway",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "format": "prettier --write \"**/*.{js,css,html,json,md}\" --ignore-path .gitignore",
    "format:check": "prettier --check \"**/*.{js,css,html,json,md}\" --ignore-path .gitignore"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  }
}
